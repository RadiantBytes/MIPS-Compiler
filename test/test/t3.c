#include <stdio.h>
#include <math.h>

    int i;
    int j;
    int k;
    int m;
    read(i, j, k, m);
    print(i, j, k, m);
    printline();
    print(pow(i,j) * k + m);
    print(pow(i,j) * k + m);
    printspaces(8);
    print(i, j, k, m);
    printline();
